# Cursor Rules - Desafio Técnico Full Stack Hubfy.ai

## 📋 Contexto do Projeto

Este é um sistema completo de gestão de tarefas desenvolvido como desafio técnico para a Hubfy.ai. O projeto utiliza **Next.js 14+ full stack** com App Router, integrando frontend e backend em uma única aplicação.

## 🏗️ Arquitetura

### Estrutura do Projeto
- **Framework**: Next.js 14+ com App Router
- **Gerenciador de Pacotes**: pnpm (recomendado) ou npm
- **Banco de Dados**: MySQL 8+ via Prisma ORM
- **Autenticação**: JWT (jsonwebtoken)
- **Validação**: Zod (backend e frontend)
- **Formulários**: React Hook Form com validação Zod
- **Estado do Servidor**: TanStack Query (React Query)
- **Styling**: Tailwind CSS 3.4+

### Estrutura de Pastas
```
projeto/
├── src/
│   ├── app/
│   │   ├── api/                    # Next.js API Routes
│   │   │   ├── auth/
│   │   │   │   ├── login/route.ts
│   │   │   │   └── register/route.ts
│   │   │   └── tasks/
│   │   │       ├── route.ts       # GET e POST
│   │   │       └── [id]/route.ts  # PUT e DELETE
│   │   ├── login/page.tsx
│   │   ├── register/page.tsx
│   │   ├── dashboard/page.tsx
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   └── globals.css
│   ├── components/                 # Componentes React reutilizáveis
│   ├── lib/
│   │   ├── db.ts                   # Prisma Client singleton
│   │   ├── auth.ts                 # Funções de autenticação
│   │   ├── middleware.ts           # Middleware de autenticação
│   │   ├── api/                    # Funções de chamada à API
│   │   │   ├── auth.ts
│   │   │   └── tasks.ts
│   │   ├── hooks/                  # Custom hooks
│   │   │   ├── use-auth.ts
│   │   │   └── use-tasks.ts
│   │   ├── providers/              # Providers React
│   │   │   └── query-provider.tsx
│   │   └── schemas/                 # Schemas Zod
│   │       └── auth.schema.ts
│   └── types/                       # Tipos TypeScript compartilhados
│       └── index.ts
├── tests/
│   ├── api/                         # Testes de API Routes
│   │   ├── auth.test.ts
│   │   └── tasks.test.ts
│   └── components/                  # Testes de componentes
├── prisma/
│   └── schema.prisma               # Schema do Prisma
├── database/
│   └── schema.sql                   # Schema SQL (referência)
├── .env.example
├── README.md
├── API.md
├── package.json
├── tsconfig.json
├── next.config.js
├── tailwind.config.ts
├── jest.config.js
└── jest.setup.js
```

## 🛠️ Stack Tecnológica

### Frontend & Backend (Full Stack Next.js)
- **Next.js 14+** (App Router)
- **React 18+**
- **TypeScript 5.3+**
- **Prisma ORM** (MySQL)
- **Zod** (validação)
- **React Hook Form** (formulários)
- **TanStack Query** (gerenciamento de estado e cache)
- **Tailwind CSS** (styling)
- **bcrypt** (hash de senhas)
- **jsonwebtoken** (autenticação JWT)

### DevOps & Ferramentas
- **Jest** (testes)
- **ESLint** (linting)
- **Prettier** (formatação)

## 🔧 Convenções de Código

### TypeScript
- **Sempre usar TypeScript** - evitar `any`, usar tipos explícitos
- **Interfaces/Types**: Definir em `src/types/index.ts`
- **Strict Mode**: Habilitado

### Nomenclatura
- **Arquivos**: kebab-case (ex: `auth.service.ts`, `task-card.tsx`)
- **Componentes React**: PascalCase (ex: `TaskCard.tsx`)
- **Classes/Interfaces**: PascalCase (ex: `AuthService`, `CreateTaskDto`)
- **Variáveis/Funções**: camelCase (ex: `getUserTasks`, `isAuthenticated`)
- **Constantes**: UPPER_SNAKE_CASE (ex: `JWT_SECRET`, `API_BASE_URL`)

### Next.js App Router
- **API Routes**: Criar em `src/app/api/` seguindo a estrutura de pastas
- **Server Components**: Preferir Server Components quando possível
- **Client Components**: Sempre marcar com `'use client'` quando necessário
- **Componentes**: Criar componentes reutilizáveis em `src/components/`
- **Hooks Customizados**: Criar em `src/lib/hooks/`
- **API Calls**: Criar funções em `src/lib/api/` para chamadas à API
- **Validação**: Usar Zod para validação de dados (backend e frontend)
- **Formulários**: Sempre usar React Hook Form com validação Zod
- **Estado do Servidor**: Usar TanStack Query para dados do servidor
- **Estado Local**: Usar useState/useReducer apenas para estado UI local

### Arquitetura em Camadas
- **API Routes** (`src/app/api/`): Controllers - apenas roteamento e validação
- **Lib Services** (`src/lib/`): Lógica de negócio (auth.ts, etc.)
- **Prisma** (`src/lib/db.ts`): Camada de dados (ORM)
- **Hooks** (`src/lib/hooks/`): Abstração de lógica reutilizável
- **Types** (`src/types/`): Definições de tipos compartilhados

## 🔒 Segurança

### Obrigatório
- **Senhas**: Sempre hash com bcrypt (salt rounds: 10)
- **JWT**: Tokens com expiração configurável (padrão: 24h) e token_version
- **Validação**: Validar todos os inputs com Zod (backend e frontend)
- **SQL Injection**: Usar Prisma ORM - NUNCA SQL raw sem sanitização
- **Variáveis Sensíveis**: Sempre usar `.env` - NUNCA commitar credenciais
- **Autenticação**: Proteger todas as rotas de API que precisam autenticação

### Isolamento de Dados
- Usuários só podem acessar suas próprias tarefas
- Validar `user_id` em todas as operações de tarefas
- Usar middleware de autenticação em todas as rotas protegidas

## 📝 Padrões de Desenvolvimento

### Commits
- **OBRIGATÓRIO**: Fazer um commit separado para cada arquivo alterado/criado
- Cada alteração deve ter seu próprio commit com mensagem descritiva
- Usar Conventional Commits
- Exemplos:
  - `feat: adiciona página de login`
  - `fix: corrige validação de email`
  - `refactor: reorganiza estrutura de componentes`
  - `test: adiciona testes para auth routes`
  - `docs: atualiza documentação da API`
- **Regra de Commits Separados**:
  - Se você modificar múltiplos arquivos, criar um commit para cada arquivo
  - Se criar novos arquivos, cada arquivo novo deve ter seu próprio commit
  - Commits devem ser atômicos e focados em uma única mudança
  - Exemplo: Se você adicionar validação em 5 arquivos, fazer 5 commits separados

### Testes
- **API Routes**: Testes obrigatórios para endpoints da API
- **Cobertura mínima**: 60% dos endpoints
- **Localização**: `tests/api/`
- **Ferramentas**: Jest + React Testing Library
- **Frontend**: Testes de componentes com React Testing Library

### Formatação
- **Prettier**: Configurado no projeto
- **ESLint**: Configurado para Next.js
- **Formatação automática**: Executar `pnpm format` antes de commitar
- **Configuração**: `.prettierrc` e `.prettierignore` na raiz

## 🗄️ Banco de Dados

### Schema
- **ORM**: Prisma (`prisma/schema.prisma`)
- **Tabelas**:
  - `users`: id, name, email, password, token_version, created_at
  - `tasks`: id, user_id, title, description, status, created_at, updated_at
- **Índices**: Criados para otimização (email, user_id, status)
- **Foreign Keys**: user_id referencia users(id) ON DELETE CASCADE

### Conexão
- **Prisma Client**: `src/lib/db.ts` (singleton pattern)
- **Configuração**: Via variáveis de ambiente (DATABASE_URL)

## 🌐 API Endpoints

### Autenticação
- `POST /api/auth/register` - Registro de usuários
- `POST /api/auth/login` - Login e obtenção de JWT

### Tarefas (Protegidas)
- `GET /api/tasks` - Listar tarefas do usuário
- `POST /api/tasks` - Criar nova tarefa
- `PUT /api/tasks/:id` - Atualizar tarefa
- `DELETE /api/tasks/:id` - Deletar tarefa

### Documentação
- Ver `API.md` para documentação completa dos endpoints

## 🔄 Fluxo de Desenvolvimento

### 1. Configuração Inicial
```bash
# Instalar dependências
pnpm install

# Gerar Prisma Client
pnpm prisma generate

# Configurar banco de dados
docker-compose up -d
# ou
mysql -u root -p < database/schema.sql

# Executar migrações Prisma (se necessário)
pnpm prisma migrate dev

# Configurar variáveis de ambiente
cp .env.example .env
# Editar .env com credenciais
```

### 2. Desenvolvimento
```bash
# Executar servidor de desenvolvimento
pnpm dev

# Build de produção
pnpm build

# Executar produção
pnpm start
```

### 3. Testes
```bash
# Todos os testes
pnpm test

# Testes em modo watch
pnpm test:watch

# Cobertura de testes
pnpm test:cov
```

### 4. Prisma
```bash
# Gerar Prisma Client
pnpm prisma generate

# Criar migração
pnpm prisma migrate dev

# Abrir Prisma Studio
pnpm prisma studio

# Push schema (desenvolvimento)
pnpm db:push
```

## 📦 Variáveis de Ambiente

### .env
```env
# Next.js
NODE_ENV=development
PORT=3000

# Database (Prisma)
DATABASE_URL="mysql://root:root@localhost:3306/task_manager"

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=24h
```

## 🎯 Decisões Técnicas Importantes

### Por que Next.js Full Stack?
- Arquitetura unificada simplifica desenvolvimento
- API Routes integradas com App Router
- Server Components para melhor performance
- Deploy simplificado (uma única aplicação)
- Compartilhamento de tipos entre frontend e backend

### Por que Prisma?
- Type-safe queries
- Migrations automáticas
- Geração automática de tipos TypeScript
- Melhor DX (Developer Experience)
- Suporte a múltiplos bancos de dados

### Por que React Hook Form?
- Performance otimizada (menos re-renders)
- Validação integrada com Zod
- Melhor UX (validação em tempo real)
- Menos código boilerplate

### Por que TanStack Query?
- Cache automático de requisições
- Sincronização de estado do servidor
- Refetch automático
- Otimistic updates
- Melhor gerenciamento de loading/error states

## 🚫 O Que NÃO Fazer

- ❌ Commitar arquivos `.env` com credenciais reais
- ❌ Armazenar senhas em texto plano
- ❌ Usar SQL raw sem sanitização (usar Prisma)
- ❌ Deixar endpoints sem autenticação quando necessário
- ❌ Usar `any` no TypeScript sem necessidade
- ❌ Ignorar tratamento de erros
- ❌ Criar componentes muito grandes (dividir em menores)
- ❌ Hardcode de valores (usar variáveis de ambiente)
- ❌ Misturar lógica de negócio nas API Routes (usar lib/)
- ❌ Criar formulários sem React Hook Form
- ❌ Fazer chamadas diretas à API sem TanStack Query

## ✅ Checklist de Qualidade

Antes de commitar, verificar:
- [ ] Código formatado com Prettier
- [ ] Sem erros de lint
- [ ] TypeScript sem erros
- [ ] Testes passando
- [ ] Variáveis de ambiente não commitadas
- [ ] Commits com mensagens descritivas
- [ ] Código segue as convenções do projeto
- [ ] Formulários usam React Hook Form
- [ ] Chamadas à API usam TanStack Query
- [ ] Validação com Zod implementada

## 📚 Recursos Úteis

- [Next.js Docs](https://nextjs.org/docs)
- [Prisma Docs](https://www.prisma.io/docs)
- [React Hook Form Docs](https://react-hook-form.com/)
- [TanStack Query Docs](https://tanstack.com/query/latest)
- [Zod Docs](https://zod.dev/)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [JWT.io](https://jwt.io/)

## 🎨 Padrões de UI/UX

### Frontend
- **Responsividade**: Mobile-first com Tailwind CSS
- **Loading States**: Sempre mostrar indicadores de loading
- **Error Handling**: Mensagens de erro claras e amigáveis
- **Validação**: Validação em tempo real nos formulários (React Hook Form + Zod)
- **Feedback Visual**: Feedback imediato para ações do usuário
- **TanStack Query**: Usar estados isLoading, isError, isSuccess para UI

### Cores e Estilo
- Usar classes Tailwind CSS
- Manter consistência visual
- Seguir design system quando aplicável

## 🔄 Padrões de Código

### API Routes
```typescript
// src/app/api/example/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';
import { authenticateRequest } from '@/lib/middleware';

const schema = z.object({
  // schema definition
});

export async function GET(request: NextRequest) {
  const authResult = await authenticateRequest(request);
  if (authResult instanceof NextResponse) return authResult;
  
  // lógica
  return NextResponse.json({ data });
}
```

### React Hook Form
```typescript
// src/app/example/page.tsx
'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { schema, FormData } from '@/lib/schemas/example.schema';

export default function ExamplePage() {
  const { register, handleSubmit, formState: { errors } } = useForm<FormData>({
    resolver: zodResolver(schema),
  });

  const onSubmit = (data: FormData) => {
    // handle submit
  };

  return <form onSubmit={handleSubmit(onSubmit)}>...</form>;
}
```

### TanStack Query
```typescript
// src/lib/hooks/use-example.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { getData, createData } from '@/lib/api/example';

export function useData() {
  return useQuery({
    queryKey: ['data'],
    queryFn: getData,
  });
}

export function useCreateData() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: createData,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['data'] });
    },
  });
}
```

---

**Última atualização**: Projeto refatorado para Next.js full stack com React Hook Form e TanStack Query.
